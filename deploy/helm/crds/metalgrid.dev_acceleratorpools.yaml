---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: acceleratorpools.metalgrid.dev
spec:
  group: metalgrid.dev
  names:
    kind: AcceleratorPool
    listKind: AcceleratorPoolList
    plural: acceleratorpools
    singular: acceleratorpool
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.acceleratorType
      name: Type
      type: string
    - jsonPath: .spec.placementStrategy
      name: Strategy
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          AcceleratorPool is a named slice of accelerator capacity, shared across
          namespaces like a StorageClass — not scoped to one team's namespace.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              AcceleratorPoolSpec describes a logical slice of accelerator capacity: which
              nodes belong to it and how jobs should be placed within it.
            properties:
              acceleratorType:
                description: AcceleratorType this pool provides, matching AcceleratorJob.spec.acceleratorType.
                type: string
              nodeSelector:
                additionalProperties:
                  type: string
                description: NodeSelector restricts jobs in this pool to matching
                  nodes.
                type: object
              placementStrategy:
                default: BinPack
                description: PlacementStrategy is BinPack (default) or Spread.
                enum:
                - BinPack
                - Spread
                type: string
              tolerations:
                description: Tolerations allows jobs in this pool to run on tainted
                  nodes.
                items:
                  description: |-
                    The pod this Toleration is attached to tolerates any taint that matches
                    the triple <key,value,effect> using the matching operator <operator>.
                  properties:
                    effect:
                      description: |-
                        Effect indicates the taint effect to match. Empty means match all taint effects.
                        When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                      type: string
                    key:
                      description: |-
                        Key is the taint key that the toleration applies to. Empty means match all taint keys.
                        If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                      type: string
                    operator:
                      description: |-
                        Operator represents a key's relationship to the value.
                        Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                        Exists is equivalent to wildcard for value, so that a pod can
                        tolerate all taints of a particular category.
                        Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                      type: string
                    tolerationSeconds:
                      description: |-
                        TolerationSeconds represents the period of time the toleration (which must be
                        of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                        it is not set, which means tolerate the taint forever (do not evict). Zero and
                        negative values will be treated as 0 (evict immediately) by the system.
                      format: int64
                      type: integer
                    value:
                      description: |-
                        Value is the taint value the toleration matches to.
                        If the operator is Exists, the value should be empty, otherwise just a regular string.
                      type: string
                  type: object
                type: array
            required:
            - acceleratorType
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
