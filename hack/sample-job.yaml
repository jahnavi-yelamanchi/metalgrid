apiVersion: metalgrid.dev/v1alpha1
kind: AcceleratorJob
metadata:
  name: hello-job
spec:
  image: busybox:1.36
  command: ["sh", "-c", "echo hello from metalgrid && sleep 5"]
  acceleratorType: mock-gpu
  acceleratorCount: 1
  team: platform
